#include <stdio.h>

int main() {
   printf("---->>>");
  
    int val[6];
    for (int i = 0; i < 6; i++) {
        printf("Digite o %dº valor:", i+1);
        scanf("%d", &val[i]);
    }

    printf("Os valores digitados foram:\n");
    for (int i = 0; i < 6; i++) {
        printf("%d\n", val[i]);
    }

    return 0;
}

